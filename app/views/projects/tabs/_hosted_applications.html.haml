- hosted_applications ||= [] 

-# Project Links
= tabs do
  = tab "Demo Links", "hosted_instances" do
    = partial "hosted_instances/hosted_instance", :with => hosted_applications, :as => :hosted_instance
    = check_empty(hosted_applications, "hosted versions") do
      = br
      = link_to "Add One Now...", "#", :class => "switch-to-last"
    
  = tab "add link...", "hosted_instances_add" do
    .tab-form= partial "hosted_instances/form"